name: "DEV DEPLOY"

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: dev
      aws-assume-role-arn: "arn:aws:iam::384711117426:role/github-actions-georgetonietti-pipeline"
      aws-region: "us-east-1"
      aws-statefile-s3-bucket: "georgetonietti-us-east-1-terraform-statefile"
      aws-lock-dynamodb-table: "georgetonietti-us-east-1-terraform-lock"